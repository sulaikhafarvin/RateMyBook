<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <!-- Link to Font Awesome for icons -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
    />
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
        margin: 0;
        padding: 20px;
        text-align: center;
      }

      h2 {
        margin-top: 100px;
        margin-bottom: 100px; /* Space between username and cards */
        color: #333;
      }

      .logout-button {
        position: absolute;
        top: 20px;
        right: 20px;
        text-decoration: none;
        color: white;
        background-color: #19aa8d;
        padding: 10px 20px;
        border-radius: 5px;
        transition: background-color 0.3s;
      }

      .logout-button:hover {
        background-color: #138b71;
      }

      .dashboard {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
        margin-top: 30px;
      }

      .card-link {
        text-decoration: none;
        color: inherit;
      }

      .card {
        background-color: #19aa8d;
        color: white;
        border-radius: 10px;
        padding: 40px 50px; /* Increased padding for larger cards */
        text-align: center;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s, box-shadow 0.3s;
        width: 200px; /* Increased width */
        height: 200px; /* Increased height */
        cursor: pointer;
      }

      .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
      }

      .card h3 {
        font-size: 18px;
        margin-bottom: 10px;
        font-weight: 600;
      }

      .card p {
        font-size: 28px;
        margin: 0;
        font-weight: bold;
      }

      /* Style for icons */
      .card i {
        font-size: 60px; /* Adjust size of icon */
        margin-bottom: 20px; /* Space between icon and text */
      }

      @media (max-width: 768px) {
        .card {
          width: 220px; /* Adjusted width for smaller screens */
          height: 200px; /* Adjusted height for smaller screens */
          padding: 30px 40px;
        }

        .card h3 {
          font-size: 16px;
        }

        .card p {
          font-size: 24px;
        }
      }
    </style>
  </head>
  <body>
    <h2>Welcome <%= user.username %></h2>

    <a href="/admin/logout" class="logout-button">Logout</a>

    <div class="dashboard">
      <a href="/admin/users" class="card-link">
        <div class="card">
          <!-- User Icon (Font Awesome) -->
          <i class="fas fa-user"></i>
          <h3>Total Users</h3>
          <p><%= userCount %></p>
        </div>
      </a>
      <a href="/admin/books" class="card-link">
        <div class="card">
          <!-- Book Icon (Font Awesome) -->
          <i class="fas fa-book"></i>
          <h3>Total Books</h3>
          <p><%= bookCount %></p>
        </div>
      </a>
    </div>
  </body>
</html>
